using System.Collections;
using UnityEngine;

namespace Assets
{
    abstract public class BaseItem : MonoBehaviour
    {
        //base item class, has to be implimented

        abstract public void Use();
    }
}